// Generated by CoffeeScript 1.10.0
var MOUSE_OVER, TOGGLE_CATEGORY, combineReducers, mbReducer, mouseOverOptions, objectAssign, pyramidChart, ref;

combineReducers = require('redux').combineReducers;

objectAssign = require('object-assign');

ref = require('../actions/actions.js'), TOGGLE_CATEGORY = ref.TOGGLE_CATEGORY, MOUSE_OVER = ref.MOUSE_OVER;

pyramidChart = require('./demographics-reducers.js');

mouseOverOptions = function(state, action) {
  if (state == null) {
    state = {};
  }
  switch (action.type) {
    case MOUSE_OVER:
      return objectAssign({}, state, {
        options: action.mouseOverOptions
      });
    default:
      return state;
  }
};

mbReducer = combineReducers({
  mouseOverOptions: mouseOverOptions,
  pyramidChart: pyramidChart
});

module.exports = mbReducer;
