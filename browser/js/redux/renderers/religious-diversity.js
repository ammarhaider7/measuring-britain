// Generated by CoffeeScript 1.10.0
var Provider, React, ReactDOM, ReduxThunk, ReligionBars, applyMiddleware, compose, createStore, mbReducer, ref, render, store, unsubscribe;

ReactDOM = require('react-dom');

React = require('react');

ref = require('redux'), createStore = ref.createStore, applyMiddleware = ref.applyMiddleware, compose = ref.compose;

Provider = require('react-redux').Provider;

ReduxThunk = require('redux-thunk')["default"];

ReligionBars = require('../containers/demographics/religion-bars-containers.js').ReligionBars;

mbReducer = require('../reducers/mb-reducers/religious-diversity.js');

store = createStore(mbReducer, compose(applyMiddleware(ReduxThunk), window.devToolsExtension ? window.devToolsExtension() : void 0));

unsubscribe = store.subscribe(function() {});

render = function() {
  return ReactDOM.render(React.createElement(Provider, {
    "store": store
  }, React.createElement(ReligionBars, null)), document.getElementById('rel-bar-chart-component'));
};

module.exports = render;
